# Assumptions

Generated claims grouped by confidence. Review anything that is not `Verified` before relying on it.

## Verified Facts

- Project name is `sample-node-app`.
- Project summary taken from package description.
- Languages detected by file census: JavaScript (2).
- Package manager / ecosystem: npm.
- Install dependencies: `npm install`.
- Start development server: `npm run dev`.
- Start application: `npm run start`.
- Build for production: `npm run build`.
- Run tests: `npm run test`.
- Lint: `npm run lint`.
- Entry point `src/index.js` (Node main module).
- Detected 3 environment variable name(s).
- Deployment signal: Container image build (Docker).
- Project has tests (framework unknown).
- Extracted 3 API route(s) from source.

## Reasonable Inferences

- Framework: Express.
- Data store: PostgreSQL.
- External integration: Stripe.

## Unknowns

_None recorded._

## Needs Confirmation

_See individual documents for items marked Needs confirmation._
