"""GraphQL schema used as a tracedocs test fixture."""

type User {
  id: ID!
  name: String!
}

input CreateUserInput {
  name: String!
}

enum Role {
  ADMIN
  MEMBER
}

type Query {
  users: [User!]!
  user(id: ID!): User
}

type Mutation {
  createUser(input: CreateUserInput!): User!
}
